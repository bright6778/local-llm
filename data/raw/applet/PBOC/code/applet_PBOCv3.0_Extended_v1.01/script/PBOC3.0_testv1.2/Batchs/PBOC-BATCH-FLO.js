print("\n\n\n*********************************************");
print("* PBOC Test Batch FLO");
print("*********************************************");

if (isStaticCard)
	include(INIT_E2P_PATH);
perso_process(2, APP_DC_LEVEL, SINGLE_CURRENCY);
include("..\\PBOC-FLO-001.js");
include("..\\PBOC-FLO-002.js");

if (isStaticCard)
	include(INIT_E2P_PATH);
perso_process(1, APP_DC_LEVEL, SINGLE_CURRENCY);
include("..\\PBOC-FLO-003.js");
include("..\\PBOC-FLO-004.js");

if (isStaticCard)
	include(INIT_E2P_PATH);
perso_process(4, APP_DC_LEVEL, SINGLE_CURRENCY);
include("..\\PBOC-FLO-005.js");

if (isStaticCard)
	include(INIT_E2P_PATH);
perso_process(1, APP_DC_LEVEL, SINGLE_CURRENCY);
include("..\\PBOC-FLO-006.js");

if (isStaticCard)
	include(INIT_E2P_PATH);
perso_process(4, APP_DC_LEVEL, SINGLE_CURRENCY);
include("..\\PBOC-FLO-007.js");

if (isStaticCard)
	include(INIT_E2P_PATH);
perso_process(1, APP_DC_LEVEL, SINGLE_CURRENCY);
include("..\\PBOC-FLO-008.js");

if (isStaticCard)
	include(INIT_E2P_PATH);
perso_process(2, APP_DC_LEVEL, SINGLE_CURRENCY);
include("..\\PBOC-FLO-009.js");

if (isStaticCard)
	include(INIT_E2P_PATH);
perso_process(1, APP_DC_LEVEL, SINGLE_CURRENCY);
include("..\\PBOC-FLO-010.js");
include("..\\PBOC-FLO-011.js");
include("..\\PBOC-FLO-012.js");

if (isStaticCard)
	include(INIT_E2P_PATH);
perso_process(4, APP_DC_LEVEL, SINGLE_CURRENCY);
include("..\\PBOC-FLO-013.js");

if (isStaticCard)
	include(INIT_E2P_PATH);
perso_process(1, APP_DC_LEVEL, SINGLE_CURRENCY);
include("..\\PBOC-FLO-014.js");

if (isStaticCard)
	include(INIT_E2P_PATH);
perso_process(4, APP_DC_LEVEL, SINGLE_CURRENCY);
include("..\\PBOC-FLO-015.js");

if (isStaticCard)
	include(INIT_E2P_PATH);
perso_process(3, APP_DC_LEVEL, SINGLE_CURRENCY);
include("..\\PBOC-FLO-017.js");

if (isStaticCard)
	include(INIT_E2P_PATH);
perso_process(1, APP_DC_LEVEL, SINGLE_CURRENCY);
include("..\\PBOC-FLO-018.js");

if (isStaticCard)
	include(INIT_E2P_PATH);
perso_process(3, APP_DC_LEVEL, SINGLE_CURRENCY);
include("..\\PBOC-FLO-019.js");